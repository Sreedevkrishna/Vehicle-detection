# Vehicle-detection
YOLO V5 is used to classify the vehicle according to their category.
The dataset should be created by yourself for training the YOLO for detection
First upload the dataset then run the training command and then you can test using the detection command
